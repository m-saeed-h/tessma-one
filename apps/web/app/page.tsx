export default function Home() {
  return (
    <div>
      <p>Demo slice: log in, create a customer, raise an invoice, issue it, and watch it post to a balanced ledger.</p>
      <p>Start at <a href="/login">Login</a> (demo@tessma.one / demo1234).</p>
    </div>
  );
}
